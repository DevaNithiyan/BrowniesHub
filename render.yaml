services:
  - type: web
    name: brownieshub
    env: python
    buildCommand: pip install -r requirements.txt && python database/init_db.py
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
